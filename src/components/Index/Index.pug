- const {Link} = require('react-router')
- const {slowScrollTo} = require('../../javascript/utils')
- const Gallery16 = require('./../Gallery16/Gallery16')
- const PortfolioItem = require('./../PortfolioItem/PortfolioItem')
div
    #title-screen
        .row.white-triange-bottom-border
            .corners
                h1
                    span= this.props.siteSettings.siteTitle
                h2
                    Link(to="/about", onClick=slowScrollTo("#top")) Dominik Gmiterko
                p
                    Link(to="/portfolio/programming", onClick=slowScrollTo("#top")) Coder  
                    Link(to="/portfolio/art", onClick=slowScrollTo("#top")) Artist  
                    Link(to="/about", onClick=slowScrollTo("#top")) Me
      
        Gallery16(itemType=PortfolioItem, items=this.props.projects)
    #fnav
        .row.text-center
            Link(to="/portfolio", className="button large slowScrollTo", target="#content")= this.props.translator("More projects")