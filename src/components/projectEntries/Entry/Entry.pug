- const TextEntry = require('./../TextEntry/TextEntry')
- const ImageEntry = require('./../ImageEntry/ImageEntry')
- const FileEntry = require('./../FileEntry/FileEntry')
- const VideoEntry = require('./../VideoEntry/VideoEntry')
- const GalleryEntry = require('./../GalleryEntry/GalleryEntry')
div
    if entry.type === 'image'
        ImageEntry(translator=this.props.translator, entry=entry)
    if entry.type === 'text'
        TextEntry(translator=this.props.translator, entry=entry)
    if entry.type === 'file'
        FileEntry(translator=this.props.translator, entry=entry)
    if entry.type === 'video'
        VideoEntry(translator=this.props.translator, entry=entry)
    if entry.type === 'gallery'
        GalleryEntry(translator=this.props.translator, entry=entry)