- const {slowScrollTo} = require('../../javascript/utils')
- const {Link} = require('react-router')
.navigation-wrapper
    .background
        div
        span.mask
        div
    .breadcrumbs-wrapper
        .row
            ul.breadcrumbs
                //-block breadcrumbs
            .passiveSwitch Simple menu

    nav.metroNavigation.passive
        ul
            li.hide-connection
                Link(to='/')= this.props.translator(this.props.siteSettings.siteTitle)
                         
                ul
                    li
                        Link(to='/about')= this.props.translator("About")
                                         
                        //- ul                    
                            li.force-l-t
                                Link(to='/')= this.props.translator("Blog")
                    
                    li
                        Link(to='/portfolio')= this.props.translator("Portfolio")
                                         
                        ul
                            li
                                Link(to='/portfolio/programming')= this.props.translator("Programming")

                                ul
                                    li
                                        Link(to='/portfolio/websites')= this.props.translator("Websites")
                             
                            li.force-l-b
                                Link(to='/portfolio/other')= this.props.translator("Other")

                                ul
                                    li
                                        Link(to='/portfolio/art')= this.props.translator("Art")
        .mover